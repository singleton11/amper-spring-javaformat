# Spring javaformat plugin example for Amper

It's a simple example of how to create a plugin for Amper that integrates with Spring javaformat.

## Usage

It provides 2 tasks:
- `format` - does the file modification
- `checkFormat` - checks if the file is formatted correctly: fails the build otherwise

